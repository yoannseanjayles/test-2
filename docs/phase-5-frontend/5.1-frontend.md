# Phase 5.1 — Front-end documenté (suivi des jalons)

> **Stack** : Next.js 16.2 (App Router, RSC, Turbopack) · TypeScript strict · Tailwind v4 · code dans `frontend/` (D-048).

## Jalon 1 — Socle + layout ✅ (🟡 en validation HITL)

### Livré

| Élément | Détail |
|---|---|
| **Tokens** | `src/app/globals.css` : primitives + sémantiques 4.1 en CSS custom properties, exposées en utilitaires via `@theme inline` (source unique, D-045). Focus ring universel, `prefers-reduced-motion` global, ProseStyles. |
| **Polices** | Fraunces (display/H1) + Work Sans (titres/UI) + Nunito Sans (corps) via `next/font/google` — self-hostées au build, `font-display: swap` (D-046). |
| **Primitives UI** (`components/ui/`) | Button (4 variantes × 6 états), FormField (aria-describedby, erreur/aide), Accordion (`<details>` natif, zéro JS), Badge, Chip, RatingStars (aria-label complet), PriceTag (Intl fr-FR, centimes), SectionHeading, Skeleton, Spinner. |
| **Layout** (`components/layout/`) | AnnouncementBar, Header sticky + méga-menu (hover **et** focus-within, catégories parentes cliquables D-002), MobileNav (dialog plein écran, Échap, gel du scroll, fermeture à la navigation), Footer 4 colonnes + NewsletterForm (RGPD, D-021), BottomBar mobile fixe (D-003). |
| **Navigation** | `lib/nav.ts` : source unique dérivée du sitemap 1.2 (18 sous-catégories, liens footer, messages de réassurance). |
| **Pages** | Accueil (hero M-HOME-01 en `next/image` priority + cartes univers M-HOME-03/04/05 — sections S5→S9 au jalon 2) · `/design-system` (styleguide vivant, noindex). |
| **Médias** | 28 visuels Phase 3 intégrés dans `public/media/` sous leur ID d'inventaire (mappage par titre de fichier, validé par l'utilisateur). Gap analysis : `docs/phase-3-medias/3.3-media-gap-analysis.md`. |

### Vérifié

- `npm run build` ✓ (build prod, pages statiques), `npm run lint` ✓, `tsc --noEmit` ✓.
- Rendu vérifié au navigateur (Playwright/Chromium) : desktop 1440, mobile 390, styleguide — captures partagées en session.
- Accessibilité socle : skip-link, landmarks, focus visible, cibles ≥ 44 px, `aria-expanded/aria-modal` sur le menu mobile.

### Hypothèses

- **H38** : Storybook (D-048) différé — un styleguide vivant `/design-system` en tient lieu pour l'instant (contrainte disque de l'environnement distant) ; Storybook sera ajouté quand l'environnement de CI sera monté.

## Jalon 2 — Cœur catalogue (à venir)

Pages univers (`/[animal]`), sous-catégories avec facettes, fiche produit, recherche. Prérequis : catalogue démo de 24 produits (H33) + mock API typée ; médias produits à générer ensuite (3.3 §5).

## Jalon 3 — Tunnel (à venir)

Drawer panier, page panier, checkout 3 étapes, confirmation.

## Jalon 4 — Compte + contenus (à venir)

Compte client (8 écrans), guides/éditorial, pages confiance & légal, CMP.
